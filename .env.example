DATABASE_URL="file:./dev.db"
# Required in every running environment. Generate with: openssl rand -base64 48
MANAGER_SESSION_SECRET=""
# Required only when seeding an empty non-demo database; remove the code after setup.
INITIAL_MANAGER_NAME=""
INITIAL_MANAGER_CODE=""
# Manager view idle timeout in ms (default 15000). E2E may set 1500.
# MANAGER_IDLE_MS=15000
