# Later: tareas vs slow and busy

Not in this build.

The rush view is a percent of that day’s sales by hour. It is not order counts, and it is not a live meter.

A later change can compare tareas done and progress — dishwashing, opening procedure, closing procedure — against those slow vs busy hours. Do not add that comparison until it is asked for. The sales-percent history in `HistoricalHourlySale` is the input that comparison should use.
